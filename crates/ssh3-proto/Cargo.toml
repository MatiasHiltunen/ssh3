[package]
name = "ssh3-proto"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false
description = "Rust SSH3 wire-format primitives and message codecs"

[lib]
path = "src/lib.rs"

