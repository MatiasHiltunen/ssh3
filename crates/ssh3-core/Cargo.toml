[package]
name = "ssh3-core"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false
description = "Rust SSH3 conversation and channel runtime"

[lib]
path = "src/lib.rs"

[dependencies]
async-trait = "0.1.89"
ssh3-proto = { path = "../ssh3-proto" }
tokio = { version = "1.48.0", features = ["io-util", "macros", "rt", "sync"] }

